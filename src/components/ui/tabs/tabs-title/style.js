import styled from 'styled-components';

const StyledTabsTitle = styled.div`
  cursor: pointer;
  user-select: none;

  border-radius: 20px;
`;
const StyledTabsImg = styled.img`
  width: 100%;
`;

export {StyledTabsImg, StyledTabsTitle};
